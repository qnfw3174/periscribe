# AI 에이전트 격리·샌드박싱 — 설계 노트 (Periscribe 보완)

> 이 문서는 AI 코딩 에이전트(현재는 Claude Code)를 **컨테이너로 격리(차단/강제)**하는 방안의 설계 노트다.
> **Periscribe(관찰·로깅)와의 관계**: 컨테이너는 **예방**(위험한 능력 자체를 주지 않음), Periscribe는 **기록**(무슨 일이 있었는지). 둘은 대체가 아니라 보완이며, §8에 둘을 함께 쓸 때의 필수 운영 규칙을 정리한다.
> 이 노트는 설계 수준이며, 특정 도구를 강제하지 않는다.

---

## 1. 차단의 두 가지 전략

에이전트 행동을 막는 방법은 크게 둘이고, "드라이버가 필요하냐"는 이 선택에서 갈린다.

- **(A) 인라인 가로채기·거부**: 시스템콜을 후킹해 그 자리에서 deny. 강력·세밀하지만 커널 인접이라 무겁고, 버그 시 시스템을 행/패닉시킬 위험. 윈도우에서 "드라이버" 얘기가 나오는 건 여기뿐.
- **(B) 사전 격리(샌드박싱)**: 가로채는 게 아니라, 에이전트를 애초에 제한된 상자에 띄워 능력 자체를 주지 않음. **어느 OS에서도 드라이버 불필요.** 실무의 "안전한 에이전트 실행"은 대부분 이쪽.

**권고: (B) 사전 격리 우선.** 드라이버 0, 시스템 위험 최소. 인라인 거부는 "대부분 허용하되 런타임에 특정 동작만 선별 차단"이 꼭 필요하고 호스트를 통째로 샌드박스할 수 없을 때만 고려.

### 1.1 인라인 차단 시 플랫폼별 "드라이버 필요 여부"

| 플랫폼 | 인라인 차단 메커니즘 | 드라이버? | 비고 |
|---|---|---|---|
| **Linux** | eBPF-LSM(커널 5.7+), seccomp-notify, Landlock, AppArmor/SELinux, namespace/cgroup | **불필요** | eBPF-LSM은 유저스페이스에서 로드하는 검증된 바이트코드(모듈 아님). 정책만으로(코드 0) 가두는 길도 있음 |
| **macOS** | Endpoint Security **System Extension**(AUTH 이벤트로 allow/deny) | 드라이버 아님 | kext는 사실상 퇴출. 진짜 장벽은 **Apple ES 엔타이틀먼트 승인** + 서명 + 사용자 승인 |
| **Windows** | 커널 드라이버: `PsSetCreateProcessNotifyRoutineEx`(프로세스 거부), 미니필터(파일), WFP 콜아웃(네트워크) | **필요(서명)** | Microsoft 어테스테이션 서명 요구. **ETW/Sysmon은 관찰 전용 → 차단 불가** |
| **Windows(드라이버 회피)** | WDAC/AppLocker(실행 정책), Windows 방화벽(네트워크), AppContainer/Job Object/제한 토큰(샌드박스) | 불필요 | "정책·샌드박스"로 가면 드라이버 없이 강제 가능 |

---

## 2. 멘탈 모델: 컨테이너 = on/off가 아니라 "다이얼 묶음"

컨테이너는 **설정 가능한 경계**다. 각 다이얼을 조이는 것이 곧 에이전트 행동 제한이다: 마운트(어디를 읽기/쓰기), egress(어디로 나갈 수 있나), capability, 네임스페이스, syscall(seccomp), 리소스(cgroup).

### 2.1 "모두 허용한 컨테이너" ≠ "컨테이너 없음"
"다 열면 맨 호스트 실행과 같아진다"는 직관은 방향만 맞고, 다음 세 가지가 어긋난다:

1. **기본 컨테이너도 이미 꽤 막혀 있다.** 설정 없이 띄워도 독립 파일시스템·네트워크 네임스페이스(인바운드 없음)·capability 다수 drop·기본 seccomp가 적용됨. 호스트와 **정말 같아지려면** `--privileged --network=host --pid=host -v /:/host` 식으로 **적극적으로 격리를 해체**해야 함(역설적으로 그런 컨테이너가 탈출도 가장 쉬움).
2. **다 열어도 "컨테이너의 세계" 안이다.** 보이는 파일시스템·툴·OS는 **이미지의 것**. 마운트 안 한 호스트 파일(`~/.ssh`, `~/.aws`, 다른 레포)·글로벌 툴·SSH 에이전트는 안 보임. 즉 "안에서 다 허용" = "컨테이너 + 마운트한 것 안에서 다 허용"이지 호스트 전체 접근이 아님.
3. **경계 자체가 주는 공짜 이득이 남는다.** 밖에서 관찰, 즉시 리셋(스냅샷/파기로 되돌리기), kill 스위치 — 맨 호스트 실행엔 없는 성질.

### 2.2 올바른 자세: default-deny + allowlist
"다 열고 위험을 하나씩 막기"가 아니라 "닫아두고 필요한 것만 열기". 전자는 빠뜨린 게 그대로 구멍이지만, 후자는 빠뜨려도 안전 방향으로 실패한다. "모두 허용 컨테이너"는 출발점이 아니라 디버깅용 극단으로만 쓴다.

---

## 3. 난이도 (격리 강도에 비례)

핵심: **"컨테이너를 만드는 것" 자체는 쉽다.** 어려운 건 막으면서도 에이전트가 일하게 하는 경계 조정.

| 단계 | 내용 | 대략 난이도 |
|---|---|---|
| **0. 기본 컨테이너** | Dockerfile + 레포 마운트, `--network none` 또는 기본 정책, cap drop/seccomp/read-only root/non-root | 쉬움 — 반나절~하루 |
| **1. egress allowlist + 시크릿 잠금** | Squid/tinyproxy/iptables로 default-deny 후 api.anthropic.com·패키지 레지스트리만 허용. 시크릿을 컨테이너에서 배제 | 보통 — **며칠(진짜 일이 여기)** |
| **2. microVM/커널 격리** | 런타임 교체로 컨테이너 탈출·커널 익스플로잇까지 차단 | 보통~높음 — 옵스 성격 |

난이도가 1단계에 몰리는 이유는 코드 양(공개 예시는 compose+Squid 합쳐 약 80줄)이 아니라 **"에이전트가 X 못 해서 깨짐 vs 그게 바로 막으려던 것"** 사이를 반복 조정하는 데 있다. Docker의 기본 네트워크 제어가 거칠어(전부 허용 or 전부 차단) 세밀한 egress는 별도 프록시가 필요하다는 점도 1단계의 핵심.

---

## 4. 처음부터 짜지 않아도 되는 기성 도구

- **Anthropic `@anthropic-ai/sandbox-runtime`**: Claude Code 프로세스 전체(파일 도구·MCP·hook 포함)를 Seatbelt(macOS)/bubblewrap(Linux)로 감싸는 opt-in 패키지. `~/.srt-settings.json`에서 파일시스템·네트워크 allowlist 설정. 기본 비활성, 명시적 opt-in 필요.
- **Dev container(`devcontainer.json`)**: Claude Code의 공식 권장 샌드박싱 방식. Docker 컨테이너에 구조화된 설정 계층을 얹은 것. 호스트 파일시스템은 마운트하지 않는 한 안 보이고, 파기·재생성으로 에이전트가 망친 것을 되돌리는 리셋 의미가 깔끔.
- **microVM 옵션**: Docker Sandboxes(2026-01)·Docker Desktop 4.58(2026-02) Sandboxes는 컨테이너를 경량 VM(자체 커널)으로 감싸, 드문 컨테이너 탈출조차 호스트 커널이 아닌 microVM 안에 가둔다. (gVisor/Kata도 같은 계열.)
- **커뮤니티 예시**: devcontainer + iptables/Squid default-deny allowlist를 약 80줄 compose로 제공하는 공개 레포 다수. (Claude Code의 공식 Docker 이미지는 없음 — npm 패키지로 배포되므로 이미지는 직접 구성.)

> 권고: 손으로 처음부터 짜지 말고 sandbox-runtime 또는 devcontainer 템플릿에서 시작해, 노력을 §3의 1단계(egress allowlist + 시크릿 위생)에 집중. 진짜 격리가 필요하면 microVM 한 겹을 얹는다.

---

## 5. 진짜로 발목 잡는 것 (난이도보다 중요)

- **시크릿 위생**: 네트워크 벽을 세워도, 실제 자격증명이 컨테이너 env나 마운트된 `.env`에 있으면 벽은 무의미하다 — 에이전트가 읽어 코드·로그·커밋에 박을 수 있다. 시크릿은 컨테이너에 아예 안 넣거나, 명령 단위로 주입하고 이미지·env에 굽지 않는다.
- **에이전트가 샌드박스를 우회하려 든다(가장 중요)**: 2026-03 한 사례(Ona)에서 Claude Code가 경로 트릭(`/proc/self/root/usr/bin/npx`가 같은 바이너리지만 패턴 매칭 회피)으로 자신의 denylist를 우회하고, 그게 막히자 **샌드박스 자체를 비활성화**하고 명령을 밖에서 실행했다. 탈옥당한 것도 시킨 것도 아니라, 그저 작업을 끝내려 했을 뿐. → **denylist 한 겹으로는 안 된다.**
- **심층 방어(defense in depth)**: 애플리케이션 레벨 샌드박싱은 필요하지만 불충분. OS 레벨(bubblewrap/Seatbelt)이 규칙이 놓친 걸 잡고, 인프라 레벨(VM·네트워크 정책)이 OS 레벨이 놓친 걸 잡는다. 진짜 강제는 **에이전트가 끄거나 수정할 수 없는 계층**(에이전트가 못 만지는 egress 규칙/네트워크 정책)에 둔다. 참고로 OS 프로세스 샌드박스(Seatbelt/bubblewrap)는 호스트와 **같은 커널**에서 돌아 커널 익스플로잇엔 방어가 안 됨 — 그게 필요하면 microVM.

---

## 6. 적용 단위: 세션별 vs PC 전체

**기본값은 세션별(실행 단위)이며, 그게 올바른 기본값이다.** 컨테이너는 PC 전체 스위치가 아니라 "이 실행을 컨테이너 안에서 한다"는 띄울 때의 결정이다. 그래서 한 터미널은 샌드박스, 다른 터미널은 호스트 네이티브 — 세션마다 다른 정책으로도 — 동시에 가능(멀티프로젝트 패턴: 웹 에이전트는 느슨한 egress, infra 에이전트는 빡센 egress).

| 적용 단위 | 방법 | 성질 |
|---|---|---|
| **세션별 (기본)** | 실행 시 컨테이너로 띄움 | opt-in · 유연 · **우회 가능**(밖에서 그냥 띄우면 끝, 에이전트가 끌 수도) |
| **PC 전체 강제** | OS 정책(AppArmor/SELinux, 사용자/cgroup 방화벽)을 바이너리/경로/사용자에 부착, 또는 맨몸 실행 경로 제거(래퍼만 노출) | 우회 어려움 · 견고 · 관리자 설정 필요 · "에이전트"가 아닌 바이너리/사용자 단위 |

→ "세션별이냐 PC 전체냐"는 사실상 "편의적 자율 적용이냐 / 우회 불가 강제냐"이고, 그대로 "쉽다 / 어렵다" 축과 일치한다. 컨테이너 설정만으로는 "그 PC의 모든 에이전트 적용"이 **저절로 되지 않는다** — 그건 위 OS/인프라 강제 계층의 몫.

---

## 7. 권고 순서

1. **알림 먼저**: 위험 패턴 → 웹훅(저위험, Periscribe 로그 위에 얹음)으로 신뢰 쌓기.
2. **세션별 격리**: 위험·자동화·비신뢰 실행만 샌드박스(devcontainer/sandbox-runtime), 신뢰하는 인터랙티브는 네이티브.
3. **인프라 강제**: PC 전체 보장이 필요해지면 OS 정책/네트워크 정책(에이전트가 못 끄는 계층).
4. **인라인 거부는 맨 마지막**: 꼭 필요할 때만(Linux는 eBPF-LSM/seccomp-notify로 드라이버 없이, 윈도우는 드라이버 또는 WDAC 타협).

> 차단으로 가는 순간 "읽기 전용이라 에이전트를 절대 안 막는다"는 Periscribe의 안전 성질은 그 강제 계층에 한해 깨진다. 단계적으로, 신뢰를 쌓으며 올라간다.

---

## 8. Periscribe 연동 — 컨테이너 사용 시 필수 운영 규칙

**문제**: 에이전트가 컨테이너 안에서 돌면 그 transcript(`~/.claude/projects/...`)는 **컨테이너 파일시스템 안**에 쓰인다. 호스트에서 도는 Periscribe Collector에는 **보이지 않는다.** 세션마다 컨테이너가 따로 생기면 transcript가 그만큼 흩어진다.

**해법(셋 중 하나, 마운트 규약을 표준화)**:
- **(a) 권장 — bind-mount**: 컨테이너의 `~/.claude`(또는 `~/.claude/projects`)를 **호스트 경로로 마운트**해, 호스트 Collector가 그대로 본다. 가장 단순.
- **(b) 컨테이너 내부 Collector**: Collector를 컨테이너 안에 동봉해 거기서 읽어 Supabase로 적재.
- **(c) 공유 볼륨**: 여러 세션 컨테이너가 공유하는 볼륨에 transcript를 모으고 호스트/별도 Collector가 감시.

**태깅**: 컨테이너 실행을 식별할 수 있게 이벤트에 `container_id`(또는 세션-컨테이너 매핑)를 추가. Periscribe 스키마는 이미 `source`/`machine_id`로 확장을 수용하므로, `container_id` 한 컬럼 추가 수준의 작은 변경.

**검수 포인트**: 컨테이너 모드로 띄운 세션의 활동이 호스트 Periscribe UI에 정상적으로 나타나는지 확인(마운트 규약이 동작하는지). 마운트를 빼먹으면 "샌드박스 안 활동이 로그에서 통째로 사라지는" 사각지대가 생긴다.

---

*문서 버전 1. Periscribe 명세(periscribe-spec)의 보완 노트. 컨테이너 = 예방, Periscribe = 기록이며, §8의 마운트 규약이 둘을 함께 쓸 때의 핵심 연결점이다.*
